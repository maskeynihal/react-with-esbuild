{
  "name": "react-with-esbuild",
  "version": "1.0.0",
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn build && yarn serve",
    "build": "node build.js",
    "serve": "servor ./build/ index.html --reload"
  },
  "dependencies": {
    "chokidar": "^3.5.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.12.18",
    "fs-extra": "^10.0.0",
    "servor": "^4.0.2"
  }
}
